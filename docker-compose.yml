services:
  nginx:
    image: nginx:alpine
    container_name: rio-storto-website
    ports:
      - "8086:80"
    volumes:
      - "./html:/usr/share/nginx/html:ro"
      - "./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro"
    restart: unless-stopped
