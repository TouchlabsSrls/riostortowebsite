# Reference

Screenshot, mockup e riferimenti visivi per lo sviluppo.

Questi file **non** vanno pubblicati in `httpdocs/`.

Sottocartelle generate automaticamente (es. audit visivi) possono essere ignorate da Git tramite `.gitignore`.
